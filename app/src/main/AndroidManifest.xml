<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.recipebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".ui.RecipeBaseApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeBase">
        <activity
            android:name="hu.bme.aut.recipebase.ui.activity.recipe_details.RecipeDetailsActivity"
            android:exported="false" />
        <activity
            android:name="hu.bme.aut.recipebase.ui.activity.main.MainActivity"
            android:theme="@style/Theme.RecipeBase"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>